# Copy to .env and adjust if needed.
# If you use scripts/run-mysql-docker.sh or docker-compose (host port 3307), the URL must use :3307.
DATABASE_URL="mysql://root:password@127.0.0.1:3307/earthworm_dev"
